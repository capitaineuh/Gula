# FastAPI et dépendances (runtime uniquement pour Vercel)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Base de données
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# Utilitaires
python-dotenv==1.0.0
python-multipart==0.0.7
email-validator==2.1.0

# Authentification et sécurité
fastapi-users[sqlalchemy]==12.1.3
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
authlib==1.3.0
requests==2.31.0
httpx==0.25.2

# Génération PDF
reportlab==4.0.7
pillow==10.1.0

# IA - Gemini API
google-generativeai>=0.8.0
PyPDF2==3.0.1

# Dépendances système nécessaires à certaines libs (passlib / fastapi-users)
setuptools

# Les dépendances de dev (pytest, black, mypy, flake8, isort, …)
# sont volontairement exclues pour garder la taille de la fonction
# serverless Vercel < 250 Mo. Utilisez un fichier séparé (ex: requirements-dev.txt)
# si besoin en local.
